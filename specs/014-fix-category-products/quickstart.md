# Quickstart

This feature does not require any new external dependencies, environment variables, or database migrations to be applied.

Run the project normally using the development server:

```bash
npm run dev
```
